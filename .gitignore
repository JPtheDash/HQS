# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

node_modules/
dist/
.vscode/
.DS_Store
*.log
# Personal Claude Code settings; .claude/settings.json is the shared one
.claude/settings.local.json
dist-apk/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/

# Python bytecode from any tools/ scripts
__pycache__/
*.pyc

# Play upload signing key and its passwords. Never commit these.
android/keystore/
*.jks
*.keystore
keystore.properties

# Built store artifacts
dist-store/

# Raw asset drop-zone. Files are copied into public/assets/ (which IS
# committed and served); keeping the 100+ MB raw folder out of git avoids
# bloating every clone.
src/Hanuman assets/
